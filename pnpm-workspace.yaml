packages:
  - 'apps/*'
  - 'packages/*'
  - 'packages/adapters/*'
# esbuild 是 tsx / vitest 的底层依赖，需要执行安装脚本下载对应平台的二进制
allowBuilds:
  # better-sqlite3 是原生模块，安装时需下载或编译对应平台的二进制。
  # M11 打 Windows 包时要留意：原生模块必须按目标平台重新构建。
  better-sqlite3: true
  esbuild: true
