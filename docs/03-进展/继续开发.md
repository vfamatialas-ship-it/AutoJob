# 下次接着做 —— 会话交接文档

> 本文件用于**跨机器、跨会话恢复开发**。关机、切换系统、换新会话后，先读这一份就能接上。
>
> 最后更新：2026-09-09（M5 完成时）

---

## 一、一句话现状

**M0–M5 已完成，PRD Phase 0 验收通过；下一步是 M6 Application Diff + 内容自适应。**

项目已经可以真正用了：`autojob apply <url>` 能打开浏览器、用你的真实简历把表单填出来，并在提交前停住等你核对。

---

## 二、恢复开发的三条命令

```bash
cd /home/yang/项目/秋招助手
pnpm install          # 若 node_modules 丢失
pnpm check            # lint + typecheck + test，应全绿
```

真正跑一次（需先启动 Mock 站）：

```bash
pnpm mock:serve &                      # 另开一个终端也行
pnpm autojob apply "http://127.0.0.1:4173/mock_company_a/apply.html" \
  --profile materials/profile.local.json --company "测试" --job "机器人算法工程师"
```

如果 `pnpm` 不存在：`corepack enable pnpm`
如果 Playwright 浏览器丢失：`pnpm exec playwright install chromium`
用 `pnpm autojob doctor` 可一次性确认环境三项是否就绪。

完整验证（含真实浏览器 E2E）：

```bash
pnpm test:e2e         # 应 125 passed / 4 skipped
```

---

## 三、当前进度

| 里程碑                   | 状态         | 产出                                              |
| ------------------------ | ------------ | ------------------------------------------------- |
| M0 项目地基              | ✅           | monorepo、质量闸门、`@autojob/core`、CLI 骨架、CI |
| M1 Candidate Profile     | ✅           | 完整数据模型、两条红线的代码强制、示例 Profile    |
| M2 Mock ATS 站点群       | ✅           | 7 个刁难站 + 零依赖静态服务器                     |
| M3 Form Schema + 分类器  | ✅           | DOM 提取、label 归因、规则分类器                  |
| M4 Mapping Engine        | ✅           | 四级规则优先级、Mapping Memory、warning 体系      |
| M5 浏览器填写引擎        | ✅           | Playwright 填写、SQLite 落库、CLI apply、隐私审计 |
| **M6 Diff + 内容自适应** | **← 下一步** | Application Diff 渲染、按字数生成压缩变体         |

**测试基线**（回归时对照这个数）：

```
pnpm test          15 files / 245 tests
pnpm privacy:check 108 个跟踪文件，0 处真实个人信息
pnpm test:e2e      125 passed / 4 skipped
```

4 项 skip 都是有意的：上传站全是 file input、Repeatable 站初始无字段、登录站表单被挡住，各有专项用例覆盖。

---

## 四、M6 要做什么（下次直接从这里开始）

M6 = Application Diff + 内容自适应（PRD §31 / §35 / §59）。

### 4.1 任务清单

1. **ContentAdapter**（优先做）—— 把 M5 遗留的「超长内容转人工」自动化掉。
   按字段 maxLength 生成 short100 / short300 / short500 变体。
   **硬约束**：生成文本里出现的数字必须都能在 `canonicalFacts[].metrics` 找到出处，
   否则拒绝。真实 Profile 里已有 `90.4%` `86.7%` `71.56%` `30.2%` 等指标可作基准。

2. **Application Diff 渲染** —— 素材已经齐了，`runs/<runId>/plan.json` 里
   已有完整的 `sourceRecordIds` 与 `warnings`，Diff 只需渲染，不必重新计算。
   目标形态见 PRD §35。

3. **CLI**：`autojob apply --diff` 输出结构化对照表，用户确认后再进入填写。

### 4.2 M6 的验收标准

- [ ] C 公司的 100 字「项目简介」能自动生成压缩版本并成功填入（当前是转人工）
- [ ] 内容校验器能拦住「凭空多出一个数字」的注入用例
- [ ] Diff 中每一项都能追溯到 Profile 的具体记录 id
- [ ] 非直接映射（如科研降级进项目）在 Diff 中明确标注

## 五、需要你（杨恩慧）决定的事

按紧急程度排序：

1. **确认真实 Profile 的两个推断值**（不阻塞，但填错会影响投递）
   我从简历推断了两项，需要你核对 `materials/profile.local.json`：
   - `expectedGraduationDate: 2027-06`（按 3 年制硕士推的）
   - `availableDate: 2027-07`
     其余字段均直接取自简历与 `填写秋招.docx`。

2. **M8 的第一个 ATS 选哪个**（不急，M7 时再定）
   从你 `秋招.xlsx` 的真实 URL 推断：飞书招聘 2 家（小鹏、德赛西威）、Moka 1 家（宇石空间）、自研 7 家。详见 `docs/05-调研/ATS初步识别.md`。
   PRD 原建议「Moka 或北森」，但**北森在你的样本里一次都没出现**。

3. **是否推 GitHub**（你之前说先不考虑）
   仓库已 `git init` 并有完整提交历史，`materials/` 全部排除在外。想推时直接加 remote 即可。

---

## 六、重要约定（改代码前必读）

完整版见 `CLAUDE.md`，这里只列最容易踩的：

- **`materials/` 是隐私目录**，已 gitignore，**永远不要提交**
- **测试代码禁止出现真实手机号/身份证/邮箱**，一律用虚构数据
- 所有日志必须走 `@autojob/core` 的 `createLogger`（自带 PII 脱敏）；ESLint 已禁止业务代码直接 `console.*`
- 浏览器全局（`document`/`window`）只允许出现在 `form-schema/src/raw-field.ts` 与 `browser/src/page-scripts.ts`
- 提交前跑 `pnpm privacy:check`，它已并入 `pnpm check`
- 每轮开发按六步走：查仓库 → 说明改哪些文件 → 实现**一个**小功能 → 跑 `pnpm check` → 修失败 → 总结
- **禁止为了让测试通过而删测试**

---

## 七、架构速览（避免重新读一遍代码）

```
页面 DOM
  ↓  EXTRACT_FIELDS_SCRIPT（浏览器侧，form-schema/raw-field.ts）
RawField[]
  ↓  classifyForm（form-schema/classifier.ts）
FormField[]  ← 带 semanticType + confidence
  ↓  mapForm（form-mapper/mapper.ts）
FormPlan     ← 每个字段 filled / skipped / ask_user + warnings
  ↓  fillForm（browser/filler.ts）
真实填入浏览器 → validateForm → 截图 → SQLite 落库 → **提交前停止**
  ↓  【M6 要做的】Application Diff + ContentAdapter
```

**三道闸**（记录要进某字段必须全过）：

1. Profile 的 `shouldNotMapTo`（M1 定的，规则不可解除）
2. MappingRule 的 `blockedCandidateTypes`（各级规则并集）
3. 字段置信度 ≥ 0.7

**关键包**：

| 包                           | 职责                                   |
| ---------------------------- | -------------------------------------- |
| `@autojob/core`              | Result、错误码、PII 脱敏、日志、Run ID |
| `@autojob/candidate-profile` | 数据模型 + 查询层（红线的唯一执行点）  |
| `@autojob/form-schema`       | DOM 提取、label 归因、语义分类         |
| `@autojob/form-mapper`       | 规则引擎、取值映射、warning 生成       |
| `@autojob/browser`           | Playwright 会话、状态机、填写、校验    |
| `@autojob/database`          | SQLite 投递记录与事件时间线            |

---

## 八、跨会话恢复的注意事项

- **代码全在 git 里**（分支 `main`，无远程）。M0–M4 是一次性初始提交，此后每个里程碑单独提交
- **`pnpm privacy:check` 是提交前的最后一道关**。它会从 `materials/profile.local.json` 读出你的真实信息，在所有 git 跟踪文件里搜一遍。没有那份文件时退化为通用模式扫描
- **`materials/` 不在 git 里**，它在磁盘上。如果要换机器或重装系统，**记得单独备份这个目录**（简历、证书、论文、专利、投递记录都在里面）
- `node_modules/` 和 Playwright 浏览器不在 git 里，重装后跑一次 `pnpm install` + `pnpm exec playwright install chromium` 即可
- 详细的每轮记录在 `docs/03-进展/PROGRESS.md`，最新的在最上面
